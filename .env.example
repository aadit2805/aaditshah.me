# Required — powers the /terminal AI chat (src/app/api/chat/route.js)
ANTHROPIC_API_KEY=

# Optional — distributed rate limiting for the chat endpoint.
# If unset, the app falls back to an in-memory limiter (see src/lib/ratelimit.js).
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

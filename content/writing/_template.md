---
slug: example-slug
title: "Example title — keep this tight"
date: "2026-05-25"
summary: "One sentence preview shown on the notes index page."
---

Write the note body here in plain markdown. **Bold**, *italics*, `inline code`, lists, links — all supported.

## Subheadings work too

```js
// code blocks render with monospace
console.log('hello');
```

> Quotes are styled with a left border.

That's it. The slug determines the URL: `/notes/<slug>`. The date drives ordering (newest first). The summary appears on the index.
